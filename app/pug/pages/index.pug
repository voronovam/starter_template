doctype html
html(lang="ru-RU")
  head
    title Start Template
    link(href="https://fonts.googleapis.com/css?family=PT+Mono|PT+Sans:400,700&display=swap&subset=cyrillic" rel="stylesheet")
    meta(name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no")
    link(href="./css/main.min.css" rel="stylesheet")
    script(async, defer, src='./js/libs.min.js')

  body
    div.content-body
      header.header
        h1 Start Template
        h2 Using Pug, Sass (scss), Gulp
        p U can do it!

    script(async, defer, src='./js/main.js')
